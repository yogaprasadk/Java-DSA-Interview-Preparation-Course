in c++,when you create m*n matrix, a block of m*n memory is allocated and everything is stored at contiguous location.
but in java m*n matrix is stored at contiguous memory location and ensure that and create a 1 dimension array of size m * n

